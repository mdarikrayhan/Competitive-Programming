#include <bits/stdc++.h>
using namespace std;
const char nl = '\n';
void solve()
{
    long long int num;
    cin >> num;
    cout<<1<<" "<<num-1<<nl;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int T = 1;
    cin >> T;
    while (T--)
    {
        solve();
    }
    return 0;
}