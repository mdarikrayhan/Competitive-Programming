#include <bits/stdc++.h>

#define int int64_t
using namespace std;

void solve() {
    double a;
    cin >> a;
    cout << log2(a);
}

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t{1};
//    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
