// Codeforces: Dice Rolling
// author: uncoded

#include <bits/stdc++.h>
using namespace std;

int t, x;

int32_t main() {
    scanf("%d", &t);
    while (t--) {
        scanf("%d", &x);
        printf("%d\n", x >> 1);
    }
    return 0;
}
